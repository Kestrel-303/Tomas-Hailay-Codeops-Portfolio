# React & vite
vite is a modern way of setting up react(npm) locally.
React is javascript library(framework) that simplifies writing a large of number of codes and reduces number of renders to only one.
